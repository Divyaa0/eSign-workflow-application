export interface PDFRepository {
    // saveFile(file: Express.Multer.File)4
    saveFile()

    // findById(id: string): Promise<PDFDocument | null>;
    // update(pdf: PDFDocument): Promise<void>;
  }
  